{% set name = "gglasso" %}
{% set version = "0.1.10" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: e67e39858b24e783155e1fc7aa85209a22afac550605ed23f288a9f7cc9c1fb9

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - pip
    - python >=3.7
    - setuptools

  run:
    - python >=3.7
    - numpy >=1.17.3
    - scipy >=0.11.0
    - scikit-learn >=0.24.1
    - numba >=0.46.0
    - pandas
    - matplotlib-base
    - seaborn
    - networkx
    - decorator ==4.4.2
    - regain

test:
  imports:
    - gglasso
  requires:
    - pip
  commands:
    - pip check

about:
  home: https://github.com/fabian-sp/GGLasso
  summary: "Algorithms for Single and Multiple Graphical Lasso problems."
  description: |
    This package contains algorithms for solving General Graphical Lasso (GGLasso) problems, 
    including single, multiple, as well as latent Graphical Lasso problems.
  license: MIT
  license_family: MIT
  license_file: LICENSE.txt
  doc_url: https://gglasso.readthedocs.io/en/latest/
  dev_url: https://github.com/fabian-sp/GGLasso

extra:
  recipe-maintainers:
    - Vlasovets
